#!/usr/bin/env python

''' interactive txyzoom --
- called on command line, uses dummy data and prints to stdout
'''

from pycurrents.plot.txyzoom import test

if __name__ == '__main__':
    S = test()
